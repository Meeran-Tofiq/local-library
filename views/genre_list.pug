extends layout 

block content 
  if genre_list.length 
    ul 
      each genre in genre_list 
        li 
          a(href=genre.url) #{genre.name}
  else 
    p There are no genres on this page yet