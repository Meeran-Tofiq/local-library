extends layout

block content

  h1 #{title}: #{bookinstance.imprint}

  p Do you really want to delete this Author?

  form(method='POST')
    div.form-group
      input#authorid.form-control(type='hidden', name='bookinstance_id', value=bookinstance._id )

    button.btn.btn-primary(type='submit') Delete
